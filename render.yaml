services:
  - type: web
    name: ai-video-studio
    env: node
    plan: free
    buildCommand: npm install && node build.js
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
